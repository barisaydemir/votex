[package]
name = "votex_prob"
version = "0.1.0"
edition = "2021"
description = "Votex Prob Engine — localhost hesaplama robotu (L0–L6)"
authors = ["Digital Future Tech"]

[[bin]]
name = "VotexProb"
path = "src/main.rs"

[lib]
name = "votex_prob"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
